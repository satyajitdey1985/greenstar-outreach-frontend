import { CityModule } from './city.module';
describe('CityModule', function () {
    var cityModule;
    beforeEach(function () {
        cityModule = new CityModule();
    });
    it('should create an instance', function () {
        expect(cityModule).toBeTruthy();
    });
});
//# sourceMappingURL=city.module.spec.js.map